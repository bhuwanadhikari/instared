export type SingleImage = {
  imageUrl: string;
  caption: string;
};

export const createSinglePost = ({ imageUrl, caption }: SingleImage) => {
  //TODO: create container
  //TODO: publish container
};
